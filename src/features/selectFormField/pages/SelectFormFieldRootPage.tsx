import React from "react";
import DisplayCard from "../../core/displayCard/DisplayCard";
import FormCompositionRoot from "../components/FormCompositionRoot";

const SelectFormFieldRootPage = (): JSX.Element => {
  return <DisplayCard children={<FormCompositionRoot />} />;
};

export default SelectFormFieldRootPage;
